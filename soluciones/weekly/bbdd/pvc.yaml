apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
  namespace: laravel
spec:
  resources:
    requests:
      storage: 1Gi
  storageClassName: mysql-sc
  accessModes:
    - ReadWriteOnce
